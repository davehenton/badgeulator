<p id="notice"><%= notice %></p>

<h1>Listing Sides</h1>

<table>
  <thead>
    <tr>
      <th>Design</th>
      <th>Order</th>
      <th>Orientation</th>
      <th>Margin</th>
      <th>Width</th>
      <th>Height</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sides.each do |side| %>
      <tr>
        <td><%= side.design %></td>
        <td><%= side.order %></td>
        <td><%= side.orientation %></td>
        <td><%= side.margin %></td>
        <td><%= side.width %></td>
        <td><%= side.height %></td>
        <td><%= link_to 'Show', side %></td>
        <td><%= link_to 'Edit', edit_side_path(side) %></td>
        <td><%= link_to 'Destroy', side, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Side', new_side_path %>
