%h1 Sides

%table.table.table-striped
  %thead
    %tr
      %th Design
      %th Order
      %th Orientation
      %th Margin
      %th Width
      %th Height
      %th

  %tbody
    - @sides.each do |side|
      %tr
        %td= side.design.name
        %td= link_to side.side_name, side
        %td= side.orientation_name
        %td= side.margin
        %td= side.width
        %td= side.height
        %td
          = link_to 'Edit', edit_side_path(side), class: 'btn btn-xs btn-default'
          = link_to 'Delete', side, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-xs btn-danger'

= will_paginate @sides, renderer: BootstrapPagination::Rails

.form-buttons
  = link_to 'New Side', new_side_path, class: "btn btn-default" 
  = link_to 'Designs', designs_path, class: "btn btn-default"
